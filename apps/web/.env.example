NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
ARXIV_COACH_DB_PATH=/root/.openclaw/state/arxiv-coach/arxiv-coach.db
PAPERBRIEF_BASE_URL=https://paperbrief.vercel.app
OPENROUTER_API_KEY=
SESSION_SECRET=  # random 32-char string for signing session cookies
CRON_SECRET=

# Stripe (set in Vercel dashboard — do NOT commit real values)
STRIPE_SECRET_KEY=sk_live_...          # Stripe Dashboard → Developers → API keys
STRIPE_WEBHOOK_SECRET=whsec_...        # Stripe Dashboard → Webhooks → signing secret
STRIPE_PRICE_ID_PRO=price_...          # Stripe Dashboard → Products → Pro Monthly ($12)

# Email
RESEND_API_KEY=re_...
MAGIC_LINK_SECRET=                     # random 32-char string for invite token HMAC
